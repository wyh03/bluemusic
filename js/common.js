/**
 * 补充 searchFilter 函数
 * 从参数 list 中获取所有 title （值如：“好不容易”） 字段包含参数 key （值如：“好”）的歌曲数据，并存入数组 result 中返回
 * 若没有匹配数据，则返回空数组
 * 
 */
const searchFilter = (key, list) => {
  let result = []; // 用于返回搜索结果
  // TODO：在此处补充代码实现需求

  return result;
};

module.exports = searchFilter;
