const searchFilter = require("./common.js");
const songList = require("./songList.js"); //歌曲列表数据
/**
 * 封装单元测试函数 checkerSearchFilter 用于检测 searchFilter 函数的功能是否正确
 * 若 searchFilter 函数未按照需求封装，则最终返回 false；
 * 若 searchFilter 函数封装正确，则最终返回 true。
 */
const checkerSearchFilter = () => {
  // TODO：按照需求补全代码
};
console.log(checkerSearchFilter()); // 检测需要，请勿删除
